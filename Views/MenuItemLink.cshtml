@{
    var items = Enumerable.Cast<dynamic>((System.Collections.IEnumerable) Model);
}
@if (items.Any()) {
    <a href="@Model.Href" class="dropdown-toggle" data-toggle="dropdown">@Model.Text <b class="caret"></b></a>
}
else {
    <a href="@Model.Href">@Model.Text</a>
}
